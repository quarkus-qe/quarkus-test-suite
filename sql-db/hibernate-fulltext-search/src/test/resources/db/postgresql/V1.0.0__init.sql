CREATE TABLE "base".known_fruits
(
  id   INT,
  name VARCHAR(40)
);

CREATE TABLE "company1".known_fruits
(
  id   INT,
  name VARCHAR(40)
);

CREATE TABLE "company2".known_fruits
(
  id   INT,
  name VARCHAR(40)
);