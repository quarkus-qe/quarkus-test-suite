name: test-project
services:
  db:
    image: mirror.gcr.io/library/mysql:9.4
    ports:
      - '3306'
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_USER: myuser
      MYSQL_PASSWORD: mypassword
      MYSQL_DATABASE: mydb